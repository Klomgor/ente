import { Typography } from "@mui/material";

export const PasteHero = () => (
    <Typography
        sx={{
            fontSize: { xs: "2rem", md: "2.3rem" },
            lineHeight: 1.05,
            fontWeight: 700,
            color: "#f4f7ff",
        }}
    >
        Ente Paste
    </Typography>
);
